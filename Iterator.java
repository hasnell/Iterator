package csce247.assignments.iterator;
//noah snell

public interface Iterator {

	public boolean hasNext();
	public Object next();
	/**
	 * Checks if there is another song then moves to it
	 */
}